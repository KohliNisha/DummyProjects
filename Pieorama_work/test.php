aaaaaaaaaaaaaaaaaaaaaaaaaaaa
<?php
	/*
	$email="joshiramit87@gmail.com";//username or apiKey
	$password="8message";//pwd or X
	$data = array(
		 "user" => array("email"=>"baljit.s@idsil.com","name"=>"Baljit")
	);
	
	$jsondata= json_encode($data);
	echo "START....<br /> ";
	
	$header[] = "Content-type: application/json";
	$connection = curl_init();
	curl_setopt($connection, CURLOPT_RETURNTRANSFER, true);
	curl_setopt($connection, CURLOPT_HTTPHEADER, $header);
	curl_setopt($connection, CURLOPT_HEADER, false); 
	curl_setopt($connection, CURLOPT_USERPWD, "joshiramit87@gmail.com:8message");   
	curl_setopt($connection, CURLOPT_POST, 1);
	curl_setopt($connection, CURLOPT_POSTFIELDS, $jsondata);
	//curl_setopt($connection, CURLOPT_VERBOSE, 1);
	//replace your domain url below.
	curl_setopt($connection, CURLOPT_URL, "https://joshiramit87assist.freshdesk.com/contacts.json");
	$response = curl_exec($connection);
	
	echo "Result : ".$response;
	
	$result=json_decode($response);
	echo "<pre>";
		print_r($result);
	echo "</pre>";
	*/
?> 